# frozen_string_literal: false

RSpec.describe Proposal::Classifier do
  it "has a version number" do
    expect(Proposal::Classifier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
